{
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npx prisma migrate resolve --applied 0_init || true && npx prisma migrate deploy && node prisma/seed.js && node src/index.js"
  }
}
